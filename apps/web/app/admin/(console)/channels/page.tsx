import { AdminPage, EmptyState } from "@/components/admin/admin-page";
import { getAdminRepository } from "@/server/admin/repository";
import { getSession } from "@/server/auth";
import { ChannelCreateForm, RemoveChannelButton } from "@/components/admin/admin-controls";
export default async function ChannelsPage() { const [channels,session] = await Promise.all([getAdminRepository().listChannels(),getSession()]); const canWrite=session?.role!=="viewer"; return <AdminPage title="頻道管理" description="管理推薦頻道、黑名單、權重與推薦理由。"><ChannelCreateForm canWrite={canWrite}/><div className="admin-panel">{channels.length?<table className="admin-table"><thead><tr><th>頻道</th><th>清單</th><th>權重</th><th>理由</th><th>操作</th></tr></thead><tbody>{channels.map(c=><tr key={c.id}><td>{c.title}<br/><span className="admin-muted">{c.handle??c.sourceChannelId}</span></td><td><span className={`admin-badge${c.listType==="blacklisted"?" admin-badge--danger":""}`}>{c.listType}</span></td><td>{c.trustWeight}</td><td>{c.recommendationReason??"—"}</td><td><RemoveChannelButton id={c.id} title={c.title} canWrite={canWrite}/></td></tr>)}</tbody></table>:<EmptyState>尚無頻道規則。</EmptyState>}</div></AdminPage>; }
