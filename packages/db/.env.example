DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_learning_radar

# Optional owner creation during `npm run db:seed`. The value must already be
# a password hash accepted by the web authentication layer (never plaintext).
ADMIN_OWNER_EMAIL=
ADMIN_OWNER_NAME=
ADMIN_OWNER_PASSWORD_HASH=
