{
  "name": "ai-learning-radar",
  "private": true,
  "workspaces": ["apps/web", "packages/db", "packages/shared"],
  "scripts": {
    "build": "npm run build --workspace @ai-learning-radar/web",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": { "node": ">=20" }
}
